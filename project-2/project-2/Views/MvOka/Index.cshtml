@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<Domain.cMvOka>

@{
    ViewData["Title"] = "Mintavétel oka";
}
<partial name="_ListingHeader" model="Model.Count()" />

<table class="table table-striped stickyheader">
    <thead>
        <tr>
            <th>
                Mintavétel oka
            </th>
            <th>
                Leírás
            </th>
            <th>
                Létrehozva
            </th>
            <th>
                Utolsó módosítás
            </th>
            <th class="actioncol"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MvOk)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Leiras)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Created)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastModified)
                </td>
                <td class="actioncol">
                    <partial name="_ListingActions" model="@item.Id" />
                </td>
            </tr>
        }
    </tbody>
</table>
